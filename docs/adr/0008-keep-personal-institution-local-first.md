# Keep Personal Institution local-first

Personal Institution will store its records on the user's own machine by default. Its raw observations can be sensitive, so privacy, ownership, and offline use take priority over immediate cloud access; synchronization and export will be introduced only as deliberate, separately governed capabilities.
