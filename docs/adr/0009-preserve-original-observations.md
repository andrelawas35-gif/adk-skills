# Preserve original observations

An Observation's original text is immutable after capture. Corrections are user-authored Observation revisions, rather than overwrites, so the archive can support reliable citations, accountable interpretation, and an agent that cannot silently change the record.
