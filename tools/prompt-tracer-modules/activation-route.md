# Activation route

This conditional module is loaded only after the user explicitly activates a
signal. It routes the accepted capture to `conduct-work-object` and carries no
implementation, deployment, export, or unrelated mutation authority.
