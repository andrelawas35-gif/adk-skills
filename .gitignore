# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
.venv/

# Work Studio — private operational records
.work-studio/active.md
.work-studio/inbox.md
.work-studio/objects/
.work-studio/adapter.lock
# .work-studio/config.md is tracked (workspace identity)

# Generated runtime installations; canonical artifacts live in adapters/
.agents/skills/
.claude/skills/
.copilot/skills/
.github/skills/

# Legacy single-platform lock (superseded by per-platform locks)
# Per-platform adapter locks use relative paths and are tracked

# Runtime tracer output (WO 2026-08-14-008, Decision 3) -- operational, not canonical
runtime/traces/

# Runtime checkpoint DBs (WO 2026-08-15-008, Decision 2) -- gitignored durability
# Also covers runtime/checkpoints/research_notes/, the default research-notes
# log directory (WO 2026-08-17-016 Decision 6) -- not canonical.
runtime/checkpoints/
