# asset.design.create-review-approve-pattern Asset Record

**Work Object:** `2026-08-22-017`  
**Pipeline:** `references/DESIGN-ASSET-PIPELINE.md`  
**Status:** draft  
**Asset ID:** `asset.design.create-review-approve-pattern`  
**Asset kind:** ux-pattern  
**Source of truth:** draft ingest proposal from explicit input: real-use ingest testing slice from accepted ReviewBadge tracer scope  
**Projection status:** projections are read-only and must not be edited as asset truth.

## Asset Summary

Draft UX-pattern asset for the create-review-approve flow used to test ingest of reusable behavior, states, and interaction guidance separately from visual assets.

## Lifecycle

| Step | Owning skill | Evidence |
|------|--------------|----------|
| Intake and identity | `design-manage-assets` | Draft asset record proposed from explicit ingest input; current frontier is `experience-patterns`. |

## Verification Notes

- This is a draft ingest proposal, not an accepted canonical asset.
- `design-manage-assets` must classify the record and route the next frontier.
- Creative approval, implementation, component registration, external sync, and projection publishing remain separate authority boundaries.

## Rollback

Delete this draft asset record if it has not been accepted. If accepted later,
retire it through the governing Work Object instead of deleting history.
